# Canon Compression Packet: R3 Modes

Status: accepted_into_canon / compact_r3_modes / active canon
Date: 2026-05-28
CSP event: `projects/syntax/events/2026-05-28-codex-theory-archive-r3-closure.md`

Source IDs:

- `rtheory-2026-05-28-recursive-choice-symbolic-rupture-export`
- `rtheory-2026-05-28-r3-1-3-export`
- `rtheory-2026-05-28-r3-modes-instruction-runtime-patch`
- `rtheory-2026-05-28-r3-modes-instruction-runtime-review`

## Purpose

This packet closes the internal R3 review pass by separating the compact R3
canon candidate from the larger instruction-runtime patch.

The packet was accepted on 2026-05-28 and `CANON.md` now includes the compact
R3 addition. The wider instruction-runtime material remains outside R core
canon.

## Compression Target

Only two claims are mature enough for canon-compression review:

1. R levels describe where learning cost is paid.
2. R3 has three operating modes: online control, local error repair, and
   offline retrospective rule rewrite.

Everything else from the wider patch remains outside R core canon.

## Proposed Canon Addition

If accepted, add a short section like this to `01_R_THEORY/CANON.md`:

```text
## R-levels as displaced learning

R-levels are not merely degrees of abstraction. They describe ways of moving
learning away from immediate real-world error.

R0 reacts before a meaningful choice window opens.
R1 learns through action/result feedback.
R2 simulates local trajectories before action.
R3 analyzes and rewrites the rules that generated behavior.

## R3 operating modes

R3 can operate in three compact modes:

- R3-A / online control: keeps the current prediction-action loop aligned with
  observed reality.
- R3-B / local error repair: explains a mismatch and rewrites the local rule
  that produced the error.
- R3-C / offline retrospective rewrite: reopens past errors or unresolved
  patterns when the current stream is quiet enough for deeper rule repair.

These are operating modes of R3, not new R-levels.
```

## Why This Belongs In R Theory

This compression strengthens the existing active canon without changing its
shape:

- `CANON.md` already defines R0/R1/R2/R3 and Delta-T.
- `VERSIONS/v3.1_full.md` already contains R3 modes.
- `VERSIONS/VERSION_COMPARISON_v3.1_v3.2.md` already identifies
  learning-displacement and R3 modes as future canon-review material.
- The 2026-05-28 R3 chat export gives direct provenance for the R3.1/R3.2/R3.3
  split.

The progress delta is not a new grand theory. It is a clearer internal
architecture for the existing R3 layer.

## Checklist

| Gate | Result | Note |
|---|---|---|
| Patch exists | pass | Candidate patch and review file exist. |
| Source/provenance exists | pass | Private ChatGPT export source notes exist. |
| At least one case/model anchor exists | partial pass | Internal examples and prior R3/PNA drafts exist; external cognitive source cards still needed. |
| Limitation exists | pass | R3 modes are operating modes, not R4 and not neuroscience facts. |
| Cross-link exists if needed | pass | PNA, SynTax, Culture, and GYMKiDs boundaries are recorded. |
| No level mixing | pass | Motivational field, Q-R3, decoherence, and instruction runtime are kept outside R core canon. |
| Progress delta is clear | pass | Explains R3 as online/local/offline rule repair modes. |
| Complexity cost acceptable | pass | Adds three compact mode labels, not a large mechanism. |
| Canon update can be short | pass | Proposed insertion is small and stable. |

## What Stays Outside R3 Canon

Do not include these in the R3 canon update:

- motivational field as a PNA/organism-state bridge;
- instruction packet/runtime as a full model;
- ritual, superstition, and bad-instruction distinctions;
- Q-R3 as a question engine;
- recursive-choice/decoherence analogy;
- R4 as a new operator;
- GYMKiDs/Fly Zone applied trainer methodology.

The only R3-local residue from instruction runtime is the small kernel:

```text
R3 repairs a rule; R1 stabilizes execution of the repaired instruction.
```

That kernel can be used in later Culture or GYMKiDs work, but it is not needed
for the compact R3 canon update.

## Acceptance Decision

Current decision:

```text
accepted_into_canon
compact R3 section added to CANON.md
instruction runtime remains outside R core canon
```

Acceptance event:

```text
projects/syntax/events/2026-05-28-codex-theory-archive-r3-canon-acceptance.md
```

If not accepted:

```text
superseded by acceptance
```

## Closure Result

R3 is now internally closed and accepted into active canon for this export pass:

- R3 core candidate is separated from cross-route material.
- R4 is not introduced.
- Instruction runtime does not enter R3 canon.
- The next work is route-specific split of non-R3 material, not more extraction
  from the same export batch.
